# 팰린드롬 문제
# 글자를 거꾸로 해도 뜻이 되게 만들기.



class Solution:
    def isPalindrome(self, s: str) -> bool:
        
        str=[]
        
        for char in s:
            if char.isalnum():  # isalnum은 글자와 숫자만 허용하는 함수.
                
                strs.append(char.lower())
                
        
        while len(Strs) > 1:
            if strs.pop[0] != strs.pop()
            
            return False
        
        
        return True
        
